# AGENTS.md

**Read [`CLAUDE.md`](CLAUDE.md) before changing anything here.** It is this repo's
guidance for any agent, not just Claude: what the project is, the three rules that
are expensive to undo, the commands, the architecture, and what to do after a
change.

This file is a pointer rather than a copy on purpose. The same guidance in two
files drifts, and the copy is the one that gets read while being wrong —
`CLAUDE.md` itself sets that rule: one home per rule, never document the same
thing twice.
